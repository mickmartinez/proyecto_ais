<h1>Listing especialidads</h1>

<table>
  <tr>
    <th>Codigo</th>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @especialidads.each do |especialidad| %>
  <tr>
    <td><%= especialidad.codigo %></td>
    <td><%= especialidad.nombre %></td>
    <td><%= link_to 'Show', especialidad %></td>
    <td><%= link_to 'Edit', edit_especialidad_path(especialidad) %></td>
    <td><%= link_to 'Destroy', especialidad, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Especialidad', new_especialidad_path %>
