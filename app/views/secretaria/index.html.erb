<h1>Listing secretaria</h1>

<table>
  <tr>
    <th>Belongs to</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @secretaria.each do |secretarium| %>
  <tr>
    <td><%= secretarium.belongs_to %></td>
    <td><%= link_to 'Show', secretarium %></td>
    <td><%= link_to 'Edit', edit_secretarium_path(secretarium) %></td>
    <td><%= link_to 'Destroy', secretarium, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Secretarium', new_secretarium_path %>
